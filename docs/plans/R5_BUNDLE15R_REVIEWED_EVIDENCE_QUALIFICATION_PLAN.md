# R5 Bundle 15R — Reviewed Official Evidence Qualification and Selective Golden-Case Activation

## Executive decision

The next bundle should not rebuild the Writer or create another golden-regression
contract. Bundle 14R already supplies those controls. The missing runtime link
is:

```text
reviewed official evidence
→ qualified operating drivers and research questions
→ overlap and forecast bridge checks
→ independent valuation eligibility
→ semantic and generation-lock verification
→ Bundle 14R qualification YAML
→ selective real-case execution or targeted backflow
```

Bundle 15R implements that link from exact baseline `60f3e24`.

## Workstream A — Baseline and worktree preservation

1. Verify `HEAD == 60f3e24af8572faaf1c7a9b12a37b4ac085d7b36`, or stop.
2. Record `git status --short`, including pre-existing manifest edits, deleted
   ZIP state, local evidence, and generated files.
3. Create `codex/r5-bundle15r-reviewed-evidence-qualification`.
4. Apply only the add-only Bundle 15R patch.
5. Confirm all pre-existing paths are byte-identical and remain unstaged.

Exit criterion: the patch adds only its declared paths and does not touch the
Bundle 13R issuer workflow, Bundle 14R runtime, or unrelated local work.

## Workstream B — Install and validate the compiler

1. Compile the Python module and CLI.
2. Run the focused Bundle 15R test suite.
3. Run the empty-pack seed twice.
4. Compare every generated byte.
5. Confirm four cases remain pending, no case is research-ready, and all release
   flags remain false.
6. Run the existing Bundle 14R focused tests and seed lane.

Exit criterion: malformed, unreviewed, nonofficial, sample-derived, stale,
hash-mismatched, duplicate-conflicting, or release-escalating inputs fail closed.

## Workstream C — Build reviewed official evidence packs

Use the generated scaffolds only as intake templates. Fill them from physically
archived and reviewed primary evidence.

### 301217 铜冠铜箔

Required operating chain:

```text
capacity × utilization × yield × product mix × processing fee
→ unit margin
→ working capital and cash conversion
```

Priority evidence requests:

- stable batch production versus qualification by HVLP generation;
- capacity, utilization, yield, and product mix;
- processing fee or defensible bounded unit-economics bridge;
- customer certification and order-conversion timing;
- inventory, receivables, and copper-price cash-flow consequences.

### 600988 赤峰黄金

Required operating chain:

```text
ore × grade × recovery
→ saleable metal volume × realized price − unit cost
→ stripping / sustaining capex
→ free cash flow
```

Priority evidence requests:

- mine-level production, grade, recovery, and unit cost;
- realized gold/copper price definitions;
- stripping ratio and ramp schedule;
- sustaining versus growth capex;
- acquisition/control-change facts separated from operating assumptions.

### 603259 药明康德

Required operating chain:

```text
backlog × conversion + new orders
+ project-stage and commercial mix
+ capacity utilization
→ revenue, margin, and cash flow
```

Priority evidence requests:

- backlog definition, exclusions, and conversion history;
- project-stage progression and commercial mix;
- TIDES capacity, utilization, and pricing/mix evidence;
- customer/geographic concentration and regulatory exposure;
- non-recurring gains versus core cash earnings.

### 600673 东阳光

Required operating chains:

```text
quota × volume × price − unit cost
capacity × utilization × mix × processing fee
project/unit value × acceptance
IDC capacity × utilization × unit revenue − power cost
acquisition close × consolidation ratio − financing cost
```

Priority evidence requests:

- refrigerant quota, volume, price, and unit cost;
- electronic-material capacity and product mix;
- liquid-cooling project/unit-value/acceptance bridge;
- IDC capacity, utilization, revenue definition, and power cost;
- transaction closing, consolidation timing, consideration, debt, and interest.

Narrative samples may guide question coverage and emphasis only. They may not
populate values, claims, source hashes, or model inputs.

Exit criterion: every critical question is confirmed, bounded with a reviewed
method, marked context-only, or explicitly blocked.

## Workstream D — Compile qualification and resolve exceptions

1. Run Bundle 15R against the reviewed packs.
2. Inspect the evidence request queue and conflict ledger.
3. Resolve equal duplicates deterministically; never choose silently among
   conflicting reviewed values.
4. Verify all required source classes, drivers, and questions.
5. Verify revenue/gross-profit overlap elimination.
6. Verify driver-to-statement, working-capital, and cash-flow bridges.
7. Verify at least one independent valuation method per advancing case.
8. Bind semantic, input, output, and optional human-review artifacts to hashes.

Exit criterion: each case is either `qualification_ready_for_bundle14r` or has
owned, stage-routed, evidence-specific backflow.

## Workstream E — Selectively rerun Bundle 14R

1. Invoke Bundle 14R with the generated qualification directory.
2. Confirm only independently qualified cases advance.
3. Re-run the Bundle 14R semantic gate and deterministic lock.
4. Produce candidate handoffs with human review still pending unless a real
   exact-hash review exists.
5. Do not lower thresholds or synthesize missing evidence to obtain four passes.

Exit criterion: the Bundle 14R suite truthfully reports the real ready count and
preserves backflow for every blocked case.

## Workstream F — Close conservatively

Allowed engineering close before real evidence is complete:

```yaml
engineering_implementation: complete
reviewed_evidence_qualification: operational
real_case_ready_count: 0..4
sample_quality_allowed: false
p2_allowed: false
canonical_workflow_state_mutated: false
```

Research-useful close requires:

- all four real cases have physical official evidence packs;
- all hard qualification and semantic gates pass;
- deterministic reruns pass;
- exact-hash human reviews are performed under the canonical release process;
- any later status synchronization is explicitly reviewed by the
  research-orchestrator.

## Validation matrix

| Gate | Required result |
|---|---|
| Python compile | pass |
| Bundle 15R focused tests | pass |
| empty-pack seed | 4 pending, 0 ready |
| byte determinism | pass |
| Bundle 14R focused tests | pass |
| full repository tests | pass |
| Git whitespace check | pass |
| issuer/sample token scan | no sample prose used as evidence |
| canonical workflow-state mutation | none |
| sample quality / P2 | false / false |
