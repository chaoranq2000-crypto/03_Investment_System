# R5 Bundle 10R — Reader rebuild plan

Input model generation: `model_gen_r5_bundle9r_1cd42241e6a38fb3`.

The rebuild is additive and forward-only. Historical Bundle 10 artifacts remain available for regression but are stale for the current model generation.

Execution sequence:

1. bind model generation and transition state;
2. review Reader inputs and claim boundaries;
3. construct a dynamic analysis payload;
4. render generic Reader and separate traceability appendix;
5. integrate reviewed technical, sentiment, and future-event context;
6. run the non-compensating core-section gate;
7. run cross-company and negative regressions;
8. create truthful human-review handoff and synchronize workflow surfaces;
9. lock the exact Reader generation and close with retained TODOs.

Bundle 10R does not authorize sample-quality status, P2, direct investment advice, target prices, or fabricated human-review acceptance.
