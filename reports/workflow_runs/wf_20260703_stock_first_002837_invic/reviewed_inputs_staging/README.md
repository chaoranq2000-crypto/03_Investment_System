# 002837 Reviewed Input Staging

This directory stores staging artifacts generated from the manual reviewed-input
dropzone.

Current expected state:

- no accepted reviewed input rows are present;
- pending/TODO rows do not unblock gates;
- `R5_reviewed_input_staging_result.yaml` must remain
  `source_gapped_research_draft` until accepted reviewed inputs are supplied.

The source dropzone path is:

```text
data/reviewed_inputs/wf_20260703_stock_first_002837_invic/
```
