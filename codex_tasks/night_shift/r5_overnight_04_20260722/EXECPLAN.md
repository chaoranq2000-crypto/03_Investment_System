# ExecPlan — Night04

This is a living document. Update Progress, Discoveries, Decisions, and Remaining Work throughout execution.

## Objective

Turn Night03's 43 immutable candidates into a low-friction, exact-hash review system; prevalidate all
8 pointer proposals; consume authentic decisions when available; and recompute resolution truth without
crossing human or research gates.

## Phase A — Baseline and truth lock

- hard-verify source SHA, CI, PR count, tracked readout and queue;
- lock Night03 inputs and preserve historical paths;
- import 69 stable IDs and prove 43/20/6/0 taxonomy.

## Phase B — Review control plane

- batch decision schema and candidate registry;
- blank approve/reject/defer bundles;
- conflict, stale-hash, authority, partial-acceptance and replay guards;
- inbox polling without manufacturing decisions.

## Phase C — Review acceleration

- rank by unblock leverage, first-parent completion and critical path;
- build one-page review briefs for 8 evidence, 24 analysis, 3 human and 8 pointer candidates;
- include evidence/claim links, counterevidence, downstream impact and exact hashes;
- publish reviewer dashboard in YAML, Markdown and self-contained HTML.

## Phase D — Pointer prevalidation

- resolve exact paths and acceptance commands;
- generate 8 dry-run patches in child sandboxes only;
- enforce diff ceilings, run targeted tests and detect shared-file conflicts;
- preview generation locks and rollback plans;
- execute only after valid external approval.

## Phase E — Conditional execution and unlock

- consume valid decisions at startup and checkpoints;
- apply accepted evidence/analysis/human/pointer decisions through typed adapters;
- emit independent receipts;
- recompute 20 dependencies and 6 parents after every receipt;
- carry all unresolved IDs forward.

## Phase F — Validation and publication

- adversarial, determinism, crash/resume and scope tests;
- full repository regression and source-route gate;
- at least 6 workstream commits, push, final CI and remote SHA;
- morning readout and Night05 queue.

## Progress

- [x] Phase A
- [x] Phase B
- [x] Phase C
- [x] Phase D
- [ ] Phase E
- [ ] Phase F

## Discoveries

Append; do not erase historical entries.

- The packaged Night03 receipt inspector assumed flat resolution fields, while the authoritative
  Night03 readout stores them under `research_truth`; the copied task tool now accepts that exact
  nested schema without relaxing the required 0/63 values.
- The packaged bootstrap treats normal Git stderr messages as terminating PowerShell errors.  The
  worktree and package copy were inspected at each boundary and the declared seed steps were
  completed manually without cleanup or scope expansion.
- All 43 Night03 packet hashes validate against their canonical payloads.  The unified registry
  contains 8 evidence, 24 analysis, 3 human-gate, and 8 pointer candidates with no duplicate ID.
- All 43 candidates participate in the leverage ranking.  Membership analysis covers all 20
  dependency blockers, while the dashboard continues to report zero actual unlocks and 0/63 resolved.
- Eight detached child worktrees produced eight hash-bound patches.  They collapse to two unique
  semantic variants; each sandbox passed four targeted Bundle16R tests and an exact reverse-patch check.

## Decisions

Only record engineering decisions here. Human review decisions must originate externally and bind exact hashes.

- Bind the 97 tracked Night03 inputs using Git blob bytes so the baseline manifest is checkout-EOL invariant.
- Keep the original Night03 ZIP verification in a separate runtime receipt; do not copy the untracked archive
  into the delivery branch.
- Treat conflicting duplicates as invalid for that occurrence, permit independent valid records in a partial
  batch, and require an external authority registry before any record enters conditional execution.
- Retain the eight dirty dry-run worktrees through final validation.  Do not recursively clean them; the target
  branch contains only patch and test receipts, and no pointer proposal is applied without external approval.

## Remaining Work

Synchronize from `task_queue.yaml`; external gates remain visible as `blocked_external`.
