# Phase A — Baseline and authoritative queue

Hard-stop on any mismatch. Do not begin implementation if the physical Night02
receipt, final SHA, CI, queue count, goal state, or historical path audit differs
from the source contract.
