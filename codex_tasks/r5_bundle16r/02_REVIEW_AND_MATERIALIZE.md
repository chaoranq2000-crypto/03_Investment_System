# 16R-2 — Review mappings and materialize pack candidates

1. Create one reviewer-authored mapping per Bundle 14R case.
2. Bind only physically archived, hash-verified official sources.
3. Bind only reviewed claim/metric records.
4. Use exact driver and research-question IDs from the case contract.
5. Record overlap rules, stale triggers and dependencies.
6. Bind overlap, forecast, valuation, semantic and determinism blocks to exact
   physical artifacts and hashes.
7. Re-run preview mode twice and require byte-identical output trees.

Do not override numeric values in the mapping and do not force missing evidence
into a proxy.
