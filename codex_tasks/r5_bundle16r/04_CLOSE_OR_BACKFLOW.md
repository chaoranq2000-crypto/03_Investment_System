# 16R-4 — Close engineering scope or return to evidence work

Required close evidence:

- exact baseline and branch;
- intended path list;
- compile and test results;
- two-run deterministic generation ID and tree comparison;
- four-case materialization and qualification matrices;
- source, mapping and backflow queues;
- proof that pre-existing status entries are unchanged and unstaged;
- `sample_quality_allowed=false`;
- `p2_allowed=false`;
- canonical workflow-state mutation remains false.

If a case is blocked, return to the emitted owner/stage. Do not close research
quality merely because Bundle 16R engineering tests pass.
