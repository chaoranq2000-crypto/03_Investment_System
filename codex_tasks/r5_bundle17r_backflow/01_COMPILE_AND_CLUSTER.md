# 17R-BF1-1 — Compile and cluster the 63 blockers

1. Copy the manifest template outside committed source paths.
2. Fill physical paths and hashes only; policy owns the expected decision and counts.
3. Run BF1 twice and require byte-identical output trees.
4. Confirm 63 source rows remain 63 enriched issues; duplicates must retain unique occurrence IDs.
5. Inspect every fallback/manual route and assign a concrete owner, target stage, requested output, and acceptance check.
6. Keep the terminal reactivation work order blocked by all preceding work orders.
