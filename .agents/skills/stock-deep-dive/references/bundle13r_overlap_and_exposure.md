# stock-deep-dive — Bundle 13R overlap and independent exposure

T2 starts only after the required T1 denominators and driver responses are reviewed. For the canonical `002837` Bundle 12R generation, the material definitions are `room_cooling`, `cabinet_cooling`, and `data_center_liquid_cooling_related`.

For every material segment:

- qualify independent quantitative exposure;
- attach metric IDs and evidence locators;
- reconcile the segment to consolidated revenue and gross profit;
- classify each relationship as `disjoint`, `contains` or `overlaps`;
- for `contains` or `overlaps`, supply evidence-backed revenue and gross-profit adjustments;
- preserve residuals and do not hide unallocated amounts in “other”.

The 2025A room/cabinet product-line measures and the cross-cutting liquid-cooling theme must never be added together unless disjointness is proven. The 2024A liquid-cooling management comment cannot be reconciled to the 2025A product-line denominators without explicit period-compatible revenue and gross-profit deductions.
